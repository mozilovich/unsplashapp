package com.app.unsplashapp.presentation.utils.themeanimation

import androidx.fragment.app.FragmentActivity

interface ThemeAnimationHelper {
    fun showAnimation(x: Int, y: Int, activity: FragmentActivity)
}